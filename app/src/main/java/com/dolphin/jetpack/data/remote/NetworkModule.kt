package com.dolphin.jetpack.data.remote

import retrofit2.Retrofit
import retrofit2.converter.gson.GsonConverterFactory

// This NetworkModule was an alternative implementation but is not currently used
// The project uses RetrofitClient for network operations
object NetworkModule {
    // This object is not currently used by the application
    // The project uses RetrofitClient.kt instead
}